<?xml version="1.0" encoding="UTF-8"?>
<project name="SrcDemo2">
	<property name="src.dir" value="src"/>
    <property name="lib.dir" value="lib"/>
    <property name="img.dir" value="img"/>
	<property name="build.dir" value="build"/>
    <property name="launcher.dir" value="launcher"/>
    <property name="installer.dir" value="installer"/>
	<path id="classpath">
		<fileset dir="${lib.dir}" includes="**/*.jar"/>
	</path>
	<property name="classes.dir" value="${build.dir}/classes"/>
	<property name="jar.dir" value="${build.dir}/jar"/>
	<property name="jar.file" value="${jar.dir}/${ant.project.name}.jar"/>
    <property name="jar.libdir" value="${jar.dir}/lib"/>
    <property name="jar.imgdir" value="${jar.dir}/img"/>
	<property name="main-class" value="net.srcdemo.ui.SrcDemoUI"/>
	<property name="launcher.builddir" value="${launcher.dir}/build"/>
	<property name="launcher.distdir" value="${launcher.dir}/dist"/>
    <property name="launcher.precompileddir" value="${launcher.dir}/precompiled"/>
    <property name="launcher.script" value="${launcher.dir}/setup.py"/>
    <property name="installer.ant" value="${installer.dir}/nsisant.jar"/>
    <property name="installer.script" value="${installer.dir}/setup.nsi"/>
    <property name="installer.file" value="${ant.project.name}-setup.exe"/>
    <property name="svn.excludes" value="**/*.svn"/>
	<target name="clean">
		<delete dir="${build.dir}"/>
	</target>
	<target name="compile">
		<mkdir dir="${classes.dir}"/>
		<javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath"/>
	</target>
	<target name="jar">
		<mkdir dir="${jar.dir}"/>
        <copy todir="${jar.dir}" file="LICENSE"/>
        <copy todir="${jar.libdir}">
            <fileset dir="${lib.dir}" excludes="${svn.excludes}"/>
        </copy>
		<move todir="${jar.dir}">
			<fileset dir="${jar.libdir}" includes="**/*.dll"/>
		</move>
        <copy todir="${jar.imgdir}">
            <fileset dir="${img.dir}" excludes="${svn.excludes}"/>
        </copy>
		<path id="jar-classpath">
			<fileset dir="${jar.libdir}" includes="**/*.jar"/>
		</path>
		<manifestclasspath property="lib.list" jarfile="${jar.file}">
			<classpath refid="jar-classpath"/>
		</manifestclasspath>
		<jar destfile="${jar.file}" basedir="${classes.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class}"/>
				<attribute name="Class-Path" value="${lib.list}"/>
			</manifest>
		</jar>
	</target>
	<target name="mklauncher">
		<!-- Assumes python is in the path and has modules py2exe and win32all -->
		<delete dir="${launcher.builddir}"/>
        <delete dir="${launcher.distdir}"/>
        <delete dir="${launcher.precompileddir}"/>
		<exec executable="python">
			<arg value="${launcher.script}"/>
		</exec>
		<copy todir="${launcher.precompileddir}">
			<fileset dir="${launcher.distdir}" excludes="${svn.excludes}"/>
		</copy>
		<delete dir="${launcher.builddir}"/>
		<delete dir="${launcher.distdir}"/>
	</target>
	<target name="launcher">
        <copy todir="${jar.dir}">
            <fileset dir="${launcher.precompileddir}" excludes="${svn.excludes}"/>
        </copy>
	</target>
    <target name="installer">
    	<!-- Assumes makensis.exe is in the path. Note: Use Unicode NSIS, not regular NSIS! -->
        <delete file="${installer.file}"/>
        <taskdef name="nsis" classname="com.danielreese.nsisant.Task" classpath="${installer.ant}"/>
    	<nsis script="${installer.script}"/>
    </target>
</project>